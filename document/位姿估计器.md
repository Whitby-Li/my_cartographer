# IMU + Odom 融合位姿估计

概述

imu + odom 融合相对简单

* imu 旋转估计：角速度欧拉积分；加速度估计重力方向调整旋转
* odom 位移估计
  * odom 累计数据过少时，从历史 pose 队列中估计线速度
  * odom 累计 pose 较多，从 odom 队列前后 pose 估计线速度，并利用 imu 位位姿估计器估计旋转（若无 imu 数据，对应角速度替换为 odom 队列前后的 pose 估计角速度）